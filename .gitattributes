# 默认按文本处理，检出时保留仓库中的 LF，
# 避免 shell 脚本在 Windows 上被转成 CRLF 后无法在 Linux/macOS 执行
* text=auto eol=lf

# Windows 批处理必须是 CRLF，否则 cmd.exe 解析异常
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做行尾转换
*.jar binary
*.png binary
*.jpg binary
*.zip binary
